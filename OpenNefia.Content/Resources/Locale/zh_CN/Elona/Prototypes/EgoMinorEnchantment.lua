OpenNefia.Prototypes.Elona.EgoMinorEnchantment.Elona = {
    Singing = {
        NameModifier = function(name)
            return ("歌う%s"):format(name)
        end,
    },
    Servants = {
        NameModifier = function(name)
            return ("仆人的%s"):format(name)
        end,
    },
    Followers = {
        NameModifier = function(name)
            return ("追随者的%s"):format(name)
        end,
    },
    Howling = {
        NameModifier = function(name)
            return ("嚎叫%s"):format(name)
        end,
    },
    Glowing = {
        NameModifier = function(name)
            return ("闪耀%s"):format(name)
        end,
    },
    Conspicuous = {
        NameModifier = function(name)
            return ("显眼的%s"):format(name)
        end,
    },
    Magical = {
        NameModifier = function(name)
            return ("带有魔力的%s"):format(name)
        end,
    },
    Enchanted = {
        NameModifier = function(name)
            return ("破坏黑暗的%s"):format(name)
        end,
    },
    Mighty = {
        NameModifier = function(name)
            return ("强大的%s"):format(name)
        end,
    },
    Trustworthy = {
        NameModifier = function(name)
            return ("可靠的%s"):format(name)
        end,
    },
}