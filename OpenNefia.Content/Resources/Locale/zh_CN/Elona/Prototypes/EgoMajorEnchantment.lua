OpenNefia.Prototypes.Elona.EgoMajorEnchantment.Elona = {
    Silence = {
        NameModifier = function(name)
            return ("寂静的%s"):format(name)
        end,
    },
    ResistBlind = {
        NameModifier = function(name)
            return ("耐盲目的%s"):format(name)
        end,
    },
    ResistConfuse = {
        NameModifier = function(name)
            return ("耐混乱的%s"):format(name)
        end,
    },
    Fire = {
        NameModifier = function(name)
            return ("炽热的%s"):format(name)
        end,
    },
    Cold = {
        NameModifier = function(name)
            return ("冰冻的%s"):format(name)
        end,
    },
    Lightning = {
        NameModifier = function(name)
            return ("闪电的%s"):format(name)
        end,
    },
    Healer = {
        NameModifier = function(name)
            return ("治愈者的%s"):format(name)
        end,
    },
    ResistParalysis = {
        NameModifier = function(name)
            return ("耐麻痹的%s"):format(name)
        end,
    },
    ResistFear = {
        NameModifier = function(name)
            return ("耐恐惧的%s"):format(name)
        end,
    },
    ResistSleep = {
        NameModifier = function(name)
            return ("防止睡眠的%s"):format(name)
        end,
    },
    Defender = {
        NameModifier = function(name)
            return ("防御者的%s"):format(name)
        end,
    },
}