Elona.UI.ChooseNPC = {
    AgeCounter = function(age)
        return ("(%s)"):format(age)
    end,
    GoldCounter = function(gold)
        return ("%sgp"):format(gold)
    end,
    Topic = {
        Name = "Name",
        Info = "Info",
    },
    Window = {
        Title = "NPC List",
    },
    Wage = "Wage",
}
