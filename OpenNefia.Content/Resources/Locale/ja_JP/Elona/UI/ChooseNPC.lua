Elona.UI.ChooseNPC = {
    AgeCounter = function(age)
        return ("%s歳"):format(age)
    end,
    GoldCounter = function(gold)
        return ("%s gold"):format(gold)
    end,
    Topic = {
        Name = "NPCの名前",
        Info = "情報",
    },
    Window = {
        Title = "NPC一覧",
    },
    Wage = "給料",
}
